import { RouteConfig } from "./build";
import HomePage from "./pages/HomePage";

const routes: RouteConfig[] = [
    {
        route: "index",
        page: HomePage,
    },
];

export default routes;
